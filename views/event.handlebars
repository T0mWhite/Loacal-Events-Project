<div class="text-center container #004d40 teal darken-4 z-depth-4 rounded">
  <h2>{{event.title}}</h2>
</div>
<div <div class="text-center container #004d40 teal darken-4 z-depth-3 rounded">
  <p>{{event.event_time}}</p>
  <p>{{event.event_date}}</p>

  <p>Created by {{event.user.firstName}} on {{format_date event.date_created}}</p>
  <p>
  <p>
    {{!-- <button class="btn btn-primary">Volunteer for this this event!</button> --}}
  </p>
  </div>
</div>
